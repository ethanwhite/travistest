language: r
cache: packages
sudo: false

install:
  - R -e "install.packages(c('htmltab'))"

script:
  - R -e "htmltab::htmltab(doc='http://166.153.133.121/?command=TableDisplay&table=MET&records=100', sep = '')"