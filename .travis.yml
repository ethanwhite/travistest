language: r
cache: packages
sudo: false

install:
  - R -e "install.packages(c('htmltab', 'curl'))"

script:
  - R -e "curl::curl_download('http://166.153.133.121/?command=TableDisplay&table=MET&records=10', destfile='web_table.html')"
  - cat web_table.html