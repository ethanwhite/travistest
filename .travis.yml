language: r
cache: packages
sudo: false
warnings_are_errors: false

install:
  - R -e 'library(git2r); sessionInfo()'

script:
  - R -e 'repo <- git2r::repository("."); last_commit <- git2r::commits(repo)[[1]]; last_commit@summary'
